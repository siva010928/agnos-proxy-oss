global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/rules.yml

scrape_configs:
  - job_name: agnos-proxy-llm-gateway
    static_configs:
      # gateway runs on the host (poetry run) - reach it from the container
      - targets: ["host.docker.internal:8090"]
