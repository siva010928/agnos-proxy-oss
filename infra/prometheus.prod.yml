# Prometheus config for production (gateway inside Docker network)
global:
  scrape_interval: 10s
  evaluation_interval: 10s

rule_files:
  - /etc/prometheus/rules.yml

scrape_configs:
  - job_name: agnos-proxy-gateway
    static_configs:
      - targets: ["gateway:8090"]
    metrics_path: /metrics
